const initialState = {
    isLoading: false,
    data: [],
    error: {}
}

const reducerUsers = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state
    }
}

export default reducerUsers